"use strict";

function compressBoxesTwice(nums1, nums2) {
    nums1.forEach(function(msg1) {
        console.log(msg1);
    });
    
    nums2.forEach(function(msg2) {
        console.log(msg2);
    });
}

// O(n+n) 